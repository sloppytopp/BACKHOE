# BACKHOE
CLI OSINT recon tool that gives WebUI-quality results from the terminal — normalizes output from existing recon tools into one schema, scores findings by confidence/relevance, and summarizes them in plain English instead of dumping raw data.
